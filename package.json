{
  "name": "grepcode-blog",
  "private": true,
  "scripts": {
    "postinstall": "curl -L -s -o ./hugo.tar.gz https://github.com/gohugoio/hugo/releases/download/v0.160.1/hugo_extended_0.160.1_linux-amd64.tar.gz && tar xzf ./hugo.tar.gz && rm -f ./hugo.tar.gz && chmod +x ./hugo",
    "build": "./hugo"
  }
}
